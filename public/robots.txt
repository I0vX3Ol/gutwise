# robots.txt for GutWise — https://gutwise.nexudel.com
# Crawling is open. The only exclusion is the post-conversion page.

User-agent: *
Allow: /

# Conversion destination — no search value, and noindex'd in the page head.
Disallow: /thank-you/

# NOTE: /og/ is deliberately NOT disallowed. Facebook, LinkedIn and X all
# respect robots.txt when fetching og:image, so blocking that path would break
# the social preview on every page. The images are kept out of image search by
# an `X-Robots-Tag: noindex` header on /og/* instead (see _headers).

Sitemap: https://gutwise.nexudel.com/sitemap-index.xml
